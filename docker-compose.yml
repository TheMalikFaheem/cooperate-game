version: '3.8'
services:
  app:
    build: .
    container_name: cutthroat-corp
    ports:
      - "4000:4000"
    environment:
      - PORT=4000
    volumes:
      - ./data:/app/data
    restart: unless-stopped
